{
  "name": "opticip-dashboard-frontend",
  "version": "1.0.0",
  "private": true,
  "description": "JS-тесты фронтенда OptiCIP Dashboard (Vitest — чистые функции, Playwright — сценарии).",
  "scripts": {
    "test:unit": "vitest run",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "vitest": "^2.1.0"
  }
}
