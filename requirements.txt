# Веб-приложение «Отчёты по мойкам» (OptiCIP Dashboard)
# Базовые зависимости для веб-режима (run_wash_ui.py).
# Для сборки .exe версии закреплены отдельно — см. requirements-windows.txt.
fastapi>=0.110
uvicorn>=0.29
jinja2>=3.1
python-multipart>=0.0.9

# Загрузка данных по FTP использует стандартный модуль ftplib —
# дополнительные зависимости не требуются.

# Хранение паролей FTP: на Linux/macOS — системное хранилище секретов
# (Secret Service / Keychain) через keyring; при его отсутствии код сам
# откатывается на локальное base64-кодирование. На Windows не требуется (DPAPI).
keyring>=24.0

# Опционально — десктопный режим в окне (run_wash_desktop.py):
#   pywebview ставится только под Windows/macOS/Linux-GUI, поэтому вынесен отдельно.
# pywebview>=5.0
