# OptiCIP Dashboard — Roadmap

Живой документ приоритетов. Обновлять по мере закрытия пунктов.

## Контекст
- Стабильная версия — **1.1.25** (проверенный код: admin-установщик, обновление по кнопке).
- Бэкенд разбит на 15 модулей (`webapp/*.py`), `app.py` — HTTP-слой. 226 pytest + 10 vitest + e2e.
- Эксперимент per-user установки + фонового обновления **откачен** (не протестирован на Windows). Код восстановим из коммита `fc1bb53`.

---

## P0 — Тихие обновления (довести до ума)
Порядок важен: сначала подпись, потом per-user.

1. **Подпись установщика и .exe (Authenticode).** Убирает SmartScreen и «Издатель: неизвестен» в UAC; без неё любой тихий/фоновый апдейт вызывает подозрение ОС. Сертификат (OV/EV) → секрет `WINDOWS_PFX_BASE64` в CI (шаги `Sign executable`/`Sign installer` уже готовы). — *также закрывает HIGH из ревью.*
2. **Независимая проверка апдейта.** Сверять Authenticode-подпись скачанного `.exe` перед запуском (`WinVerifyTrust`/встроенный ключ) — сейчас доверие держится только на GitHub-digest; при компрометации репо/CI → тихий admin-RCE.
3. **Per-user установка + фоновое обновление** (вернуть `fc1bb53`, доработать). Тест на живой Windows: тихая установка без UAC, миграция/сосуществование со старой Program Files-копией, перезапуск. Явно решить миграцию (оставлять старую копию для отката vs. снимать).

## P1 — Аналитика мойки
4. **Пороги по температуре и времени выдержки фаз** (сейчас только концентрация).
5. **Нормативы по программам/объектам**, а не глобально.
6. **Экспорт** журнала в CSV/Excel; пакетный PDF по периоду.

## P2 — UX и надёжность
7. **Свернуть в трей** (`pystray`).
8. ~~**Локальный API-токен** для uvicorn на 127.0.0.1~~ — **готово** (см. Исправлено, итерация 5).
9. **Диагностика/логи в UI** (просмотр `desktop.log`, статус фоновых задач).

## P3 — Тех-долг и качество
10. **Разбить `app.js`** (~4.6k строк) на модули, как бэкенд.
11. **CI кросс-платформенно** (часть тестов by-design падает на Windows — вынести).

---

## Находки ревью (дыры/баги/оптимизация)

### Исправлено
Итерация 1 (поверхностный проход):
- `open_ftp_workspace`: `create_ftp_workspace` вне try → 500 при сбое диска/прав → теперь `state.error`+303.
- Скрытый поллер `/api/update/job` после закрытия «Настроек» → отменяется по токену `settingsOpenId`.
- CR/LF в именах файлов FTP (инъекция в `RETR`/`MDTM`) → отсекаются в `_ftp_list_entries`.
- `ValueError`/`LimitOverrunError` из баннера discovery → 500 → теперь ловятся.
- Затенение модуля `config` локальной переменной в роутах FTP → переименовано.

Итерация 2 (глубокий проход):
- **Порог разрыва цикла** (`wash_report.py`): мерялся по «дотянутому» `end_ts` → эффективный порог = `max_gap+period`, две мойки склеивались. Добавлено `Segment.last_sample_ts`, разрыв меряется по сырому времени (кэш workspace v6).
- **Осиротевшие `.part-<uuid>`**: `sweep_stale_part_files` подметает брошенные времянки (>30 мин) перед синхронизацией.
- **NLST + запрет SIZE**: имя, похожее на данные (`.db`/архив), теперь классифицируется как файл (не терялось бы как «каталог»).
- **Гонка переименования объектов**: `build_wash_rows` резолвит имя из снимка overrides, а не из мутируемого `cycle.object_name` (нет полу-применённого переименования в списке).
- **Гонка зеркала (MED-HIGH):** per-profile лок записи (`state.mirror_write_lock`) — один писатель на `datalog/<id>` даже при таймауте `join` (зависший поток больше не пишет параллельно с новым джобом → нет битых `.db`). `remove_ftp_profile_dir` тоже ждёт этот лок (а не «текущий» поток) перед rmtree.

Итерация 3 (hardening по итогам прохода по безопасности + адверсариальной проверки свежих правок):
- **JSON релиза GitHub без лимита** (`updates.py`): тело `releases/latest` читалось без потолка (бинарь-то капался `UPDATE_MAX_BYTES`) → добавлен `RELEASE_JSON_MAX_BYTES = 4 МБ`.
- **CRLF в FTP-пароле** (`ftp_registry.py`): единственное поле, шедшее в `login()` без app-level фильтра CR/LF (пути/имена уже чистились) → символы вырезаются в `normalize_ftp_connection_settings`.
- Адверсариальная проверка лока зеркала / `last_sample_ts` v6 / sweep / overrides подтвердила: **регрессий, дедлоков, key-mismatch и pickle-`AttributeError` нет** (сегменты пересобираются из `merged_samples`, старый `Segment` до `build_cycles` не доходит).

Итерация 4 (оптимизация + код-ревью):
- **Бэкенд-перф:** `evaluate_concentration` — серии по фазам собирались отдельным сканом сэмплов на каждую фазу → один проход (−33–50% на горячем цикле концентрации, гоняется на каждом FTP-рефреше); `build_wash_rows` — `make_cycle_key` считался 2× на строку → готовый ключ прокидывается в `resolve_cycle_default_status`; `page_context` — `resolve_default_folder_path` (чтение+`json.loads` настроек) вызывался 2× на загрузку `/` → один раз; `build_cycle_chart_payload` — проход по сэмплам на каждую серию (4×) → один проход, `round(ts*1000)` раз на сэмпл; `format_ts` — `import datetime` поднят в шапку.
- **Фронт-перф:** форс-reflow (`getBoundingClientRect` после `innerHTML`) вызывался на каждом кадре скролла → высоты меряются один раз после инвалидации окна (флаг `washListMetricsMeasured`); `moveWashSelection` пересканировал весь `displayItems` на каждую стрелку → индексы строк считаются один раз в `buildWashDisplayItems` (снимает и «O(n)-пересканы» из мелких).
- **Чистка:** удалён мёртвый код (`is_supported_archive`, `_fetch_latest_release_tag`, ~24 неиспользуемых stdlib-импорта в `app.py`); исправлен рассинхрон комментария `v5`/`CACHE_VERSION=6`.

Итерация 5 (P2-8 — локальный API-токен):
- **Аутентификация локального сервера** (`app.py`, `run_wash_desktop.py`). Loopback-guard не различает пользователей машины — на общем/RDP-хосте другой локальный юзер мог читать данные и дёргать действия через `127.0.0.1:port`. Теперь десктоп-лаунчер взводит случайный токен (`arm_api_token`, `secrets.token_urlsafe`), первичная навигация окна идёт на `/?k=<token>`, ответ ставит **HttpOnly-cookie** — дальше `fetch` и `EventSource` (не умеет слать заголовки) авторизуются автоматически как same-origin, правок каждого вызова не потребовалось. Middleware требует токен на всё, кроме `/static` (там не данные). Проверка выключена, когда токен не взведён (тесты/дев). Порядок источников токена: query → cookie → заголовок (persistent-хранилище окна на старте шлёт старый cookie вместе с новым query — query должен побеждать, иначе первая загрузка = 403). `wait_until_ready` научен считать 403 «сервер поднялся». +8 тестов.

### Открыто (крупное — в P0/P2)
- **HIGH:** неподписанный установщик + доверие только GitHub-digest (P0-1/P0-2).
- **MED:** остаточный TOCTOU файла установщика (`%LOCALAPPDATA%` без 0700 на Windows — открывать с share-deny-write); глобальный `--ignore-certificate-errors` + приватные IP в WebView (LAN MITM, частично смягчено отсутствием `js_api` у окон панелей).
- **MED:** активный `.db` без SIZE и без роста mtime тихо не докачивается (нет resume); shutdown не джойнит демон-потоки, отмена авто-refresh во время `to_thread` может стартовать джоб на завершении.

### Открыто (мелкое, некритичное)
- `make_cycle_key` обрезает ts до целых секунд (теоретическая коллизия); одно нечисловое значение метрики выбрасывает весь сэмпл; ложный «провал концентрации» при overshoot заполнения; tz заморожен через границу DST.
- `extract_archive_dbs_cached`: финальный `rglob` вне лока → LRU может вытеснить `cache_dir`; LRU может вытеснить sample-side-файлы активного анализа; лок удерживается на I/O по каталогу.
- Неотрроттленные сохранения настроек; `error_reply` рвёт весь FTP-прогон; дубль при миграции старой раскладки; частичные `.db` при срабатывании анти-бомбы.
- WebView2-флаг через `setdefault`; игнор кода выхода WebView2-бутстрапера; hover-rAF не отменяется при внешнем teardown графика; потеря фокуса строки при виртуальном скролле (виртуализация пересоздаёт DOM через `innerHTML`, FP3 — потолок оптимизации); неотменяемые in-flight fetch; повторная простановка автозапуска.

### Открыто (тех-долг из код-ревью — крупные рефакторы, не срочно)
- **Фронт:** дробление `openSettings` (~620 стр.) на `initSettings*()`; единый модуль потока обновлений (welcome-экран и «Настройки» дублируют логику `check`/`install`, welcome использует литерал `2400` вместо `UPDATE_POLL_MAX_TICKS`); хелперы `persistSetting`, `closeButtonHtml`, `createFtpModal`, `parseDbDateTime` (устраняют повторы разметки/форматирования).
- **Бэк:** дробление `download_ftp_files` (~200 стр.) и `analyze_db_files_incremental`; вынести read-side `get_diagnostics`/`_SAFE_VERSION_RE` в `views.py`/`updates.py`; общие хелперы `read_json_object` (5 копий try/except + залогировать битый файл настроек), `parse_bool_flag`, `ACTIVE_JOB_STATUSES`, `raise_if_cancelled` + константа сообщения отмены (×9).

### Проверено и чисто
XSS (экранирование + Jinja autoescape), path traversal (архивы/FTP/удаление профиля), HMAC перед pickle, SSRF discovery, sqlite (ro + закрытие в finally), ресурсы/локи/кэши, корректность разбивки на модули (нет циклов, общий `state`, точки monkeypatch).
